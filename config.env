# =============================================================================
# DGGS Benchmark Replication - Central Configuration
# =============================================================================
#
# This file defines key versions and settings used across the project.
# Edit THIS FILE to update versions - all other files reference this.
#
# Usage:
#   - Docker:         docker build .  (reads this file)
#   - Shell:          source config.env
#   - GitHub Actions: Sources this file in workflows
#   - Python:         Loaded by run_replication.py
#
# =============================================================================

# -----------------------------------------------------------------------------
# ORIGINAL BENCHMARK REPOSITORY
# -----------------------------------------------------------------------------
# The benchmark code we are replicating
# Paper: https://doi.org/10.1080/20964471.2024.2429847

DGGS_BENCHMARKS_REPO=https://github.com/manaakiwhenua/dggsBenchmarks
DGGS_BENCHMARKS_VERSION=v1.1.1

# -----------------------------------------------------------------------------
# ORIGINAL PAPER REFERENCE
# -----------------------------------------------------------------------------

ORIGINAL_PAPER_DOI=10.1080/20964471.2024.2429847
ORIGINAL_PAPER_TITLE="Using a discrete global grid system for a scalable, interoperable, and reproducible system of land-use mapping"

# -----------------------------------------------------------------------------
# DOCKER REGISTRY
# -----------------------------------------------------------------------------
# Where the Docker image is pushed by GitHub Actions
# Default: GitHub Container Registry (ghcr.io)

DOCKER_REGISTRY=ghcr.io
# Note: Full image name is ${DOCKER_REGISTRY}/${GITHUB_REPOSITORY}
#       e.g., ghcr.io/username/dggs-benchmark-replication

# -----------------------------------------------------------------------------
# PYTHON VERSION
# -----------------------------------------------------------------------------
# No specific version required - let conda find compatible version
# (conda will choose based on GDAL/geospatial library compatibility)
# If needed, uncomment and set to specific version:
# PYTHON_VERSION=3.11

# -----------------------------------------------------------------------------
# BENCHMARK PARAMETERS
# -----------------------------------------------------------------------------
# These match the paper's methodology

H3_RESOLUTION=9
RANDOM_SEED=42

# -----------------------------------------------------------------------------
# DATASET SIZES
# -----------------------------------------------------------------------------
# These control how many layers are generated and tested.
# 
# PAPER VALUES (require ~16GB+ RAM):
#   VECTOR_LAYERS=10,20,50,100,200,500,1000
#   RASTER_LAYERS=10,50,100,500,1000,5000,10000
#   POLYGONS_PER_LAYER=100
#
# CI VALUES (fit in GitHub Actions ~7GB RAM):
#   VECTOR_LAYERS=10,20,50,100,200
#   RASTER_LAYERS=10,50,100,500,1000
#   POLYGONS_PER_LAYER=50
#
# Adjust based on available memory. The benchmark demonstrates that
# traditional vector methods fail at ~500 layers due to memory,
# while DGGS scales linearly.
